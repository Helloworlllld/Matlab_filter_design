% 设定参数
L_As = 15; % 给定的带外抑制（dB）
L_Ar = ...; % 通带内回波损耗（dB）
omega_s = 0.5e9; % 偏离通带边界频率0.5GHz
omega_c = ...; % 中心频率
epsilon = sqrt(10^(L_Ar/10) - 1); % 计算ε

% 计算滤波器阶数
n = acosh(sqrt((10^(L_As/10) - 1)/(10^(L_Ar/10) - 1))) / acosh(omega_s/omega_c);
n = ceil(n); % 向上取整

disp(['滤波器的阶数为：', num2str(n)]);
